"use client";
import React,{useEffect,useMemo,useState} from "react";import {Button} from "@robotics-crm/ui";import {CheckCircle2,Phone} from "lucide-react";
type Followup={interaction_id:string;guardian_id?:string;student_id?:string;next_action_at:string;summary?:string;guardian?:{full_name:string;phone?:string;status:string};student?:{full_name:string;status:string}};
const groups=[{id:"overdue",label:"Просрочено"},{id:"today",label:"Сегодня"},{id:"week",label:"Следующие 7 дней"},{id:"later",label:"Позже"}];
function bucket(value:string){const date=new Date(value),now=new Date(),today=new Date(now.getFullYear(),now.getMonth(),now.getDate()),tomorrow=new Date(today);tomorrow.setDate(today.getDate()+1);const week=new Date(today);week.setDate(today.getDate()+8);if(date<today)return"overdue";if(date<tomorrow)return"today";if(date<week)return"week";return"later";}
export default function FollowupsPage(){const[items,setItems]=useState<Followup[]>([]),[active,setActive]=useState("overdue"),[message,setMessage]=useState("");async function load(){const response=await fetch("/api/crm/followups");const payload=await response.json();if(response.ok&&payload.ok)setItems(payload.followups||[]);else setMessage(payload.error||"Не удалось загрузить касания")}useEffect(()=>{load()},[]);const visible=useMemo(()=>items.filter(item=>bucket(item.next_action_at)===active),[items,active]);async function complete(id:string){const response=await fetch("/api/crm/interactions",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({interactionId:id})});if(response.ok){setMessage("Follow-up завершён");await load()}}return <div style={{display:"grid",gap:20}}><div><h1 style={{margin:0}}>Повторные касания</h1><p style={{color:"var(--color-text-muted)"}}>Очередь звонков и сообщений. «Не связываться» исключены автоматически.</p></div>{message&&<div className="card-crm" style={{padding:12}}>{message}</div>}<div style={{display:"flex",gap:8,flexWrap:"wrap"}}>{groups.map(group=><button key={group.id} onClick={()=>setActive(group.id)} className="form-input" style={{width:"auto",fontWeight:700,background:active===group.id?"var(--color-primary-soft)":"#fff"}}>{group.label} · {items.filter(item=>bucket(item.next_action_at)===group.id).length}</button>)}</div><div className="crm-followup-grid" style={{display:"grid",gap:12}}>{visible.length===0?<div className="card-crm" style={{padding:24}}>Нет запланированных касаний</div>:visible.map(item=><article key={item.interaction_id} className="card-crm" style={{padding:16,background:"#fff",display:"grid",gridTemplateColumns:"minmax(0,1fr) auto",gap:16}}><div><strong>{item.guardian?.full_name||item.student?.full_name||"Контакт"}</strong><div style={{fontSize:13,color:"var(--color-text-muted)"}}>{item.guardian?.phone||"Телефон не указан"} · {item.guardian?.status||item.student?.status}</div><p style={{margin:"8px 0 0"}}>{item.summary||"Связаться с клиентом"}</p><small>{new Date(item.next_action_at).toLocaleString("ru-RU")}</small></div><div style={{display:"flex",gap:8,alignItems:"center"}}><Button variant="secondary-crm"><Phone size={14}/> Позвонили</Button><Button variant="primary-crm" onClick={()=>complete(item.interaction_id)}><CheckCircle2 size={14}/> Завершить follow-up</Button></div></article>)}</div><style jsx>{`@media(max-width:640px){.crm-followup-grid article{grid-template-columns:1fr!important}.crm-followup-grid article>div:last-child{flex-wrap:wrap}}`}</style></div>}
